services:
  - type: web
    name: d24-live-dashboard
    env: python
    buildCommand: |
      pip install -r requirements.txt && playwright install chromium
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT